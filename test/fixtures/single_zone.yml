---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: intra-front-end
spec:
  podSelector:
    matchLabels:
      partition: front-end
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - ipBlock: 10.10.1.0/24
    - podSelector:
        matchLabels:
          partition: front-end
  egress:
  - to:
    - ipBlock: 10.10.1.0/24
    - podSelector:
        matchLabels:
          partition: front-end
